export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum TripStatus {
  DRAFT = "DRAFT",
  PLANNED = "PLANNED",
  ONGOING = "ONGOING",
  COMPLETED = "COMPLETED",
}

export enum Visibility {
  PRIVATE = "PRIVATE",
  PUBLIC = "PUBLIC",
}

export enum ExpenseCategory {
  TRANSPORT = "TRANSPORT",
  STAY = "STAY",
  ACTIVITY = "ACTIVITY",
  MEAL = "MEAL",
  OTHER = "OTHER",
}
